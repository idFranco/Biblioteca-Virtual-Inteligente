"""DEPRECADO — Paquete de detección por regex.

Este paquete queda marcado como deprecado a partir del rework de US-009
(2026-08-11). La auditoría de Security-Audit-MCP pasa a clasificación con LLM
vía Groq (ver ``workflow/mcp/security-audit-mcp/groq_audit.py``) y este módulo
ya NO se importa en ningún punto del código.

Se mantiene físicamente por limitación del canal de push (el MCP de GitHub solo
puede crear/actualizar archivos), pero su contenido es código muerto y no debe
volver a utilizarse. Programado para eliminación definitiva en una limpieza
posterior.
"""
