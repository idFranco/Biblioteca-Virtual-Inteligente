---
id: US-005
status: Implemented
---

# User Story — US-005 — Alquiler de libros con fecha límite y devolución que libera stock

*(Possible statuses: Draft, Planned, Approved, In Progress, Implemented, Validated, Rejected)*

## User Story

Como usuario, quiero seleccionar un libro disponible para generar un alquiler con fecha límite; y como bibliotecario, quiero registrar la devolución para cerrar el alquiler y liberar el stock en la base de datos

---

## Functional Description

- **Creación de alquiler (UC-7):** Un `Usuario` autenticado (permiso `rentals.create`) selecciona un libro disponible y genera un alquiler con fecha límite. El sistema valida: usuario autenticado, permiso `rentals.create`, libro existente, `AvailableCopies > 0`, que el usuario no tenga otro alquiler `Active` del mismo libro, y que la `DueDate` sea válida (entre hoy y máx. 30 días).
- **Stock atómico:** Al crear el alquiler se decrementa `Book.AvailableCopies` en 1 dentro de una transacción con condición `WHERE AvailableCopies > 0` (operación atómica en SQL, SQLite con WAL y `busy_timeout=5000`). Si no hay stock, responde `409 Conflict` y no se crea ningún registro.
- **Estados del alquiler:** `Active` (creado), `Returned` (devuelto), `Overdue` (devuelto después de la fecha límite). `RentalStatus` y la entidad `Rental` ya existen en el dominio.
- **Devolución (UC-12):** Solo un `Bibliotecario` o `Admin` (permiso `rentals.return`) puede registrar la devolución de un alquiler activo. Se actualiza `ReturnedAt`, el estado pasa a `Returned` (o `Overdue` si se devuelve tarde), y `Book.AvailableCopies` se incrementa en 1 (atómicamente, sin exceder `TotalCopies`). Devolver un alquiler ya devuelto resp `409`.
- **Consulta de mis alquileres (permiso `rentals.view_own`):** cualquier usuario registrado lista únicamente SUS alquileres (scope por el `userId` del claim JWT, nunca por el id enviado por el cliente).
- **Consulta admin (permiso `rentals.view_all`):** Admin/Bibliotecario listan todos los alquileres con filtros opcionales por usuario y estado.
- **Permisos:** Se agregan a los `RoleClaims` sembrados: `Admin` → `rentals.create/return/view_own/view_all`; `Bibliotecario` → `rentals.return`, `rentals.view_all`; `Usuario` → `rentals.create`, `rentals.view_own`. Se crean las policies en `Program.cs` con `RequireClaim("permission", …)`.
- **Validación:** FluentValidation antes de procesar (BookId y RentalId no vacíos, DueDate opcional con rango [hoy, hoy+30 días]).
- **Sin impacto en chatbot/MCP:** Esta historia es solo backend + frontend; los servidores MCP y el chatbot permanecen intactos (ADR-014).

---

## Criterios de aceptación

```gherkin
Escenario: Usuario alquila un libro disponible
  Given un usuario autenticado con permiso rentals.create
  And un libro con al menos 1 copia disponible
  When envía POST /api/rentals con bookId y dueDate válida
  Then resp 201 con status "Active" y dueDate registrada
  And AvailableCopies decrementa en 1

Escenario: Alquilar un libro sin stock
  Given un libro con AvailableCopies = 0
  When un usuario intenta alquilarlo
  Then resp 409 y no se crea ningún alquiler

Escenario: Fecha límite inválida
  Given un libro disponible
  When se envía dueDate en el pasado o a más de 30 días
  Then resp 400 con errores de validación
  And no se crea ningún alquiler

Escenario: Alquiler duplicado del mismo libro por el mismo usuario
  Given un usuario con un alquiler activo del libro X
  When el usuario intenta alquilar el mismo libro X
  Then resp 409 y el número de alquileres del usuario no cambia

Escenario: Registro de devolución con liberación de stock
  Given un bibliotecario con permiso rentals.return
  And un alquiler activo (libro sin copias disponibles)
  When invoca POST /api/rentals/{id}/return
  Then el alquiler pasa a status "Returned" con returnedAt
  And AvailableCopies del libro se incrementa en 1

Escenario: Devolución de un alquiler ya devuelto
  Given un alquiler con status "Returned"
  When se intenta devolver de nuevo
  Then resp 409 y el stock no cambia

Escenario: Devolución de un alquiler vencido
  Given un alquiler activo cuya dueDate ya pasó
  When un bibliotecario registra la devolución
  Then el alquiler pasa a status "Overdue" y el stock se libera

Escenario: Usuario no puede registrar devoluciones
  Given un usuario sin permiso rentals.return
  When intenta POST /api/rentals/{id}/return
  Then resp 403 y el alquiler sigue "Active"

Escenario: Usuario consulta solo sus alquileres
  Given existen donde hay que consultar
  When el usuario A consulta GET /api/rentals/my
  Then la respuesta solo incluye los alquileres del usuario A

Escenario: Bibliotecario administra alquileres
  Given un bibliotecario con permiso rentals.view_all
  When consulta GET /api/rentals
  Then recibe el listado paginado de todos los alquileres
  And puede ver el botón de registrar devolución
```

---

## Impacted Use Cases

| Use Case | Impact |
|---|---|
| `US-007` Book rental with due date | Commands/queries de creación de alquiler con reglas de stock y fecha límite. |
| `US-009` Book return | Command de devolución que libera stock y cierra el alquiler. |
| `US-002` Login with JWT | `TokenService` emite los nuevos `permission` claims desde los RoleClaims sembrados. |
| `US-003` Role and permission management | Seed de permisos `rentals.*` para los tres roles. |
| `US-004` UI conditioned by role/permission | Botones "Alquilar" y "Registrar devolución" condicionados por permisos. |

---

## Impacted Modules

| Module | Impact |
|---|---|
| `backend` | Alto: nuevos Commands/Queries/Handlers/Validators/Contracts, policies `rentals.*`, seed de permisos, `RentalsController`. |
| `frontend` | Alto: servicios, tipos, páginas "Mis alquileres" y "Gestión de a lquileres", rutas protegidas por permiso. |
| `chatbot` | None (story futura de MCP/chatbot la consumirá). |
| `Biblioteca-MCP` | None (habilitación según ADR-014 en story propia). |
| `Security-Audit-MCP` | None. |
| `Open Library MCP` | None. |

---

## Role Planning

| Role | plan |
|---|---|
| `Functional Analyst` | Funcionalidad de alquiler/devolución y casos borde. |
| `Architect` | Sin cambios de esquema; stock atómico; `ConflictException` → 409. |
| `Technical Lead` | Endpoints, permisos por rol, semántica HTTP. |
| `Backend Developer` | Handlers, CROS y validators de rentas. |
| `Frontend Developer` | Páginas de mis/quiritos, diálogo alquilar, guards. |
| `AI Engineer` | Impacto cero. |
| `QA` | Build, curl B1-B10, checklist manual. |
| `Technical Writer` | Actualizar READMEs y bitácora. |

---

## Technical Plan

### Backend (.NET, Clean Architecture + CQRS)

**Nuevos archivos:**

- `src/Application/Commands/Rentals/CreateRentalCommand.cs`.
- `src/Application/Commands/Rentals/ReturnRentalCommand.cs`.
- `src/Application/Commands/Rentals/Validators/` (FluentValidation).
- `src/Application/Queries/Rentals/GetMyRentalsQuery.cs`, `GetRequests.cs`.
- `src/Application/Contracts/Rentals/RentalContracts.cs`.
- `src/Application/Common/ConflictException.cs` → 409.
- `src/Infrastructure/Handlers/Rentals/` — handlers + mapper.
- `src/WebAPI/Controllers/RentalsController.cs`.

**Modificados:** `DependencyInjection.cs`, `Program.cs` (policies y role-claims), `GlobalExceptionHandler.cs`.

**Endpoints:**

| Endpoint | Policy | Éxito | Errores |
|---|---|---|---|
| `POST /api/rentals` | `rentals.create` | 201 | 400/401/403/404/409 |
| `POST /api/rentals/{rentalId:guid}/return` | `rentals.return` | 200 | 400/401/403/404/409 |
| `GET /api/rentals/mine` | `rentals.view_own` | 200 | 401/403 |
| `GET /api/rentals` | `rentals.view_all` | 200 | 401/403 |
| `GET /api/rentals/{rentalId:guid}` | `rentals.view` | 200 | 401/403/404 |

### Frontend (ADR-010)

`services/rentals.ts`, tipos, páginas, diálogo, rutas protegidas.

### AI

Sin cambios.

---

## Validation Plan

Compilación y smoke funcional.

## Documentation Plan

Actualizar README y bitácora AI engineering.

---

## Lifecycle Updates Required

Continuar en cada fase.

---

## Implementation Approval

Approved by user.

---

## Implementation Notes

Pending.

---

## QA Result

Pending.
