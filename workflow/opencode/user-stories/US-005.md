---
id: US-005
status: Validated
---

# User Story — US-005 — Alquiler de libros con fecha límite y devolución que libera stock

*(Possible statuses: Draft, Planned, Approved, In Progress, Implemented, Validated, Rejected)*

## User Story

Como usuario, quiero seleccionar un libro disponible para generar un alquiler con fecha límite; y como bibliotecario, quiero registrar la devolución para cerrar el alquiler y liberar el stock en la base de datos

---

## Functional Description

- **Creación de alquiler (UC-7):** Un `Usuario` autenticado (permiso `rentals.create`) selecciona un libro disponible y genera un alquiler con fecha límite. El sistema valida: usuario autenticado, permiso `rentals.create`, libro existente, `AvailableCopies > 0`, que el usuario no tenga otro alquiler `Active` del mismo libro, y que la `DueDate` sea válida (entre hoy y máx. 30 días).
- **Stock atómico:** Al crear el alquiler se decrementa `Book.AvailableCopies` en 1 dentro de una transacción con condición `WHERE AvailableCopies > 0` (operación atómica en SQL, SQLite con WAL y `busy_timeout=5000`). Si no hay stock, responde `409 Conflict` y no se crea ningún registro.
- **Estados del alquiler:** `Active` (creado), `Returned` (devuelto), `Overdue` (devuelto después de la fecha límite). `RentalStatus` y la entidad `Rental` ya existen en el dominio.
- **Devolución (UC-9):** Solo un `Bibliotecario` o `Admin` (permiso `rentals.return`) puede registrar la devolución de un alquiler activo. Se actualiza `ReturnedAt`, el estado pasa a `Returned` (o `Overdue` si se devuelve tarde), y `Book.AvailableCopies` se incrementa en 1 (atómicamente, sin exceder `TotalCopies`). Devolver un alquiler ya devuelto resp `409`.
- **Consulta de mis alquileres (permiso `rentals.view_own`):** cualquier usuario registrado lista únicamente SUS alquileres (scope por el `userId` del claim JWT, nunca por el id enviado por el cliente).
- **Consulta admin (permiso `rentals.view_all`):** Admin/Bibliotecario listan todos los alquileres con filtrosopcionales por usuario y estado.
- **Permisos:** Se agregan a los `RoleClaims` sembrados: `Admin` → `rentals.create/return/view_own/view_all`; `Bibliotecario` → `rentals.return`, `rentals.view_all`; `Usuario` → `rentals.create`, `rentals.view_own`. Se crean las policies en `Program.cs` con `RequireClaim("permission", …)`.
- **Validación:** FluentValidation antes de procesar (BookId y RentalId no vacíos, DueDate opcional con rango [hoy, hoy+30 días]).
- **Sin impacto en chatbot/MCP:** Esta historia es solo backend + frontend; los servidores MCP y el chatbot permanecen intactos (ADR-014).

---

## Acceptance Criteria

```gherkin
Scenario: Usuario alquila un libro disponible
  Given un usuario autenticado con permiso rentals.create
  And un libro con al menos 1 copia disponible
  When envía POST /api/rentals con bookId y dueDate válida
  Then resp 201 con status "Active" y dueDate registrada
  And AvailableCopies decrementa en 1

Scenario: Alquilar un libro sin stock
  Given un libro con AvailableCopies = 0
  When un usuario intenta alquilarlo
  Then resp 409 y no se crea ningún alquiler

Scenario: Fecha límite inválida
  Given un libro disponible
  When se envía dueDate en el pasado o a más de 30 días
  Then resp 400 con errores de validación
  And no se crea ningún alquiler

Scenario: Alquiler duplicado del mismo libro por el mismo usuario
  Given un usuario con un alquiler activo del libro X
  When el usuario intenta alquilar el mismo libro X
  Then resp 409 y el número de alquileres del usuario no cambia

Scenario: Bibliotecario registra la devolución y libera stock
  Given un bibliotecario con permiso rentals.return
  And un alquiler activo (libro sin copias utiles)
  When invoca POST /api/rentals/{id}/return
  Then el alquiler pasa a status "Returned" con returnedAt
  And AvailableCopies del libro se incrementa en 1

Scenario: Devolución de un alquiler ya devuelto
  Given un alquiler con status "Returned"
  When se intenta devolver de nuevo
  Then resp 409 y el stock no cambia

Scenario: Devolución de un alquiler vencido
  Given un alquiler activo cuya dueDate ya pasó
  When un bibliotecario registra la devolución
  Then el alquiler pasa a status "Overdue" y el stock se libera

Scenario: Usuario no puede registrar devoluciones
  Given un usuario sin permiso rentals.return
  When intenta POST /api/rentals/{id}/return
  Then resp 403 y el alquiler sigue "Active"

Scenario: Usuario consulta sus alquileres
  Given existen un poco de alquileres
  When el usuario A consulta GET /api/rentals/my
  Then la respuesta solo incluye los alquileres del usuario A

Scenario: Administra alquileres
  Given un bibliotecario con permiso rentals.view_all
  When consulta GET /api/rentals
  Then recibe el listado paginado de todos los alquileres
  And puede ver el botón de registrar devolución
```

---

## Impacted Use Cases

| Use Case | Impact |
|---|---|
| `UserId`: consulta mis alquiler | Return list of own rentals, scope por `userId` claim. |
| `UC-2` Login with JWT | `TokenService` emite los nuevos `permission` claims desde los RoleClaims sembrados (re-login requerido). |
| `UC-3` Role and permission management | Seed de permisos `rentals.*` para los tres roles. |
| `UC-4` UI conditioned by role/permission | Botones "Alquilar" y "Registrar devolución" condicionados por permisos. |

---

## Impacted Modules

| Module | Impact |
|---|---|
| `backend` | Alto: entidad `Rental`/`RentalStatus` ya existentes (sin cambios de esquema); nuevos Commands/Queries/Handlers/Validators/Contracts, policies `rentals.*`, seed de permisos, `RentalsController`, `ConflictException` → 409. |
| `frontend` | Alto: servicios `rentals.ts`, tipos `Rental`, páginas "Mis alquileres" y "Gestión de alquileres", botón "Alquilar" en catálogo, rutas protegidas por permiso. |
| `chatbot` | None (story futura de MCP/chatbot la consumirá). |
| `Biblioteca-MCP` | None (habilitación según ADR-014 en story propia). |
| `Security-Audit-MCP` | None. |
| `Open Library MCP` | None. |

---

## Role Planning

| Role | Plan |
|---|---|
| `Functional Analyst` | Definidos: funcionalidad de alquiler/devolución, escenarios Gherkin, casos borde. |
| `Architect` | Confirmado: sin cambios de esquema (`Rental` ya existe con `DbSet`), stock atómico con `ExecuteUpdateAsync`. |
| `Technical Lead` | Este plan consolidado; endpoints, permisos por rol. |
| `Backend Developer` | 15 archivos nuevos y 3 modificados. |
| `Frontend Developer` | Servicios, tipos, páginas, rutas/guards, header. |
| `QA` | Build + curl + checklist manual UI. |
| `Technical Writer` | Actualizar README raíz, crear `workflow/backend/README.md`. |

---

## Technical Plan

### Backend (.Clean Architecture + CQRS + Dispatcher — ADR-009)

**Nuevos archivos:**

- `CreateRentalCommand` — `CreateRentalCommand(Guid UserId, Guid BookId, DateTime? DueDate)`.
- `CreateRentalCommandValidator` — BookId/UserId no vacíos; DueDate opcional en [hoy, hoy+30].
- `ReturnRentalCommand` + Validator — `RentalId` no vacío.
- `GetMyRentalsQuery`, `GETRentalsQuery`, `GetRentalByIdQuery` — paginados, con filtros y protección de propiedad.
- `RentalContracts` — `CreateRentalRequest` y respuesta.
- `ConflictException` → 409.
- 5 handlers + mapper.
- `RentalsController` — endpoints con policies.

**Modificados:** `DependencyInjection` (registros + validators), `Program.cs` (policies y role-claims), `GlobalExceptionHandler` (mapeo 409).

**Endpoints:**

| Endpoint | Policy | Éxito | Errores |
|---|---|---|---|
| `POST /api/rentals` | `rentals.create` | 201 | 400/401/403/404/409 |
| `POST /api/rentals/{rentalId:guid}/return` | `rentals.return` | 200 | 400/401/403/404/409 |
| `GET /api/rentals` | `rentals.view_all` | 200 | 401/403 |
| `GET /api/rentals/{rentalId:guid}` | `rentals.view` | 200 | 401/403/404 |

### Frontend (ADR-010)

Archivos: `services/rentals.ts`, tipos, `MisAlquileresPage`, `AlquileresAdminPage`, `CreateRentalDialog`, rutas protegidas, botón de catálogo.

### AI

Sin cambios.

---

## Validation Plan

Compilación: `dotnet build`, `npm run build`/`npm run do`. Endpoints vía curl: casos B1–B13. Checklist manual UI. Usuarios de prueba: register (Usuario).

## Documentation Plan

Actualizar README, crear `workflow/backend/README.md`, bitácora AI engineering al validar.

---

## Lifecycle Updates Required

Continued en cada fase (Planned → Approved → In Progress).

---

## Implementation Approval

Approved by user.

---

## Implementation Notes

- Backend: `CreateRentalCommand`/`ReturnRentalCommand` + validadores (FluentValidation), 3 queries, 5 handlers + `RentalMapper`, `RentalContracts`, `ConflictException` → 409, `RentalsController` (5 endpoints con policies `rentals.*`), policies y seed de permisos en `Program.cs`, registros DI en `DependencyInjection.cs`, mapeo 409 en `GlobalExceptionHandler`.
- Stock atodo con `ExecuteUpdateAsync` + transacción: decremento `WHERE AvailableCopies > 0` (matching para concurrencia) y liberación `WHERE AvailableCopies < TotalCopies`; sin cambios en esquema.
- Frontend: `services/rentals.ts`, tipos, páginas, rutas, botón catálogo, enlaces condicionales, mensajes de error ProblemDetails.
- Builds: `dotnet build` 0 errores; `npm run build` OK; `npm run lint` OK.

---

## QA Result

- **Resultado**: PASS — todos los criterios de aceptación verificados (B1–B13) + builds.
- `dotnet build` → 0 errores. `npm run build` y `npm run lint` → OK.
- Batería: B1–B13 PASS (ver detalle en log AI engineering).
- **Estado**: Implemented → Validated (tras PR).