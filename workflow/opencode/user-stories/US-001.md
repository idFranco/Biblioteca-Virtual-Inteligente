---
id: US-001
status: In Progress
---

# User Story — US-001 — Inicializar estructura base del repositorio

*(Possible statuses: Draft, Planned, Approved, In Progress, Implemented, Validated, Rejected)*

## User Story

Como arquitecto de software, quiero inicializar la estructura base del repositorio del proyecto (Frontend React + TypeScript, Backend ASP.NET Core Web API en .NET 9, la configuración inicial de Entity Framework Core con SQLite y la estructura de carpetas/archivos base para los servidores MCP), para establecer la arquitectura limpia y el punto de partida del desarrollo sin implementar aún lógica de negocio.

---

## Functional Description

Esta User Story establece el esqueleto del proyecto sin implementar lógica de negocio. Se creará la infraestructura base para que los siguientes equipos puedan trabajar en paralelo:

- **Backend:** Solución .NET 9 con Clean Architecture (Domain, Application, Infrastructure, WebAPI), EF Core + SQLite configurado, Identity scaffolding, JWT setup, CORS, rate limiting y health checks. Sin controladores de negocio.
- **Frontend:** Proyecto Vite + React 18 + TypeScript con Tailwind CSS v3, shadcn/ui, Zustand y React Router v6. Sin rutas de negocio.
- **MCP Servers:** Estructura de carpetas para biblioteca-mcp y security-audit-mcp con server.py mínimo (FastMCP).
- **Database:** Directorio workflow/database/ con placeholder .gitkeep.
- **Docker:** docker-compose.yml raíz con servicios skeleton y Dockerfiles individuales.
- **CI/CD:** GitHub Actions workflow básico que valida compilación backend y frontend.

---

## Acceptance Criteria

```gherkin
Scenario: Backend compiles and runs
Given the repository has been initialized
When `dotnet build` is executed in workflow/backend/
Then the build succeeds with no errors

Scenario: Frontend project builds successfully
Given the repository has been initialized
When `npm run build` is executed in workflow/frontend/
Then the build succeeds

Scenario: MCP server files are present
Given the repository has been initialized
When the workflow/mcp/ directory is inspected
Then biblioteca-mcp/server.py and security-audit-mcp/server.py exist and are valid Python files

Scenario: Docker Compose builds all services
Given the repository has been initialized
When `docker compose build` is executed at the root
Then all service images build successfully

Scenario: EF Core SQLite connection works
Given the backend is configured
When the application starts
Then the SQLite database file is created in workflow/database/
```

---

## Impacted Use Cases

| Use Case | Impact |
|---|---|
| `UC-001` | Inicialización del proyecto — infraestructura base |

---

## Impacted Modules

| Module | Impact |
|---|---|
| `backend` | Creación del proyecto .NET 9 con Clean Architecture (4 capas), EF Core + SQLite, Identity, JWT |
| `frontend` | Creación del proyecto Vite + React 18 + TypeScript con Tailwind, shadcn/ui, Zustand, React Router |
| `chatbot` | Sin impacto directo (solo creación del directorio) |
| `Biblioteca-MCP` | Creación de estructura base y server.py mínimo |
| `Security-Audit-MCP` | Creación de estructura base y server.py mínimo |
| `Open Library MCP` | Sin impacto (se usa vía npx) |
| `docker` | Creación de docker-compose.yml y Dockerfiles |

---

## Role Planning

| Role | Plan |
|---|---|
| `Functional Analyst` | Definir alcance funcional y criterios de aceptación |
| `Architect` | Validar Clean Architecture (4 capas), CQRS con Dispatcher propio (no MediatR), stack frontend y FastMCP |
| `Technical Lead` | Coordinar creación de .NET solution, frontend, MCP, docker-compose, .gitignore, CI |
| `Backend Developer` | Crear solución .NET 9 y configurar EF Core + SQLite, Identity, JWT, CORS, rate limiting, healthcheck |
| `Frontend Developer` | Crear proyecto Vite + TypeScript, Tailwind, shadcn/ui, Zustand, React Router |
| `AI Engineer` | Crear estructura workflow/mcp/* y workflow/chatbot placeholder |
| `QA` | Validar builds, docker compose, archivos MCP y arranque del backend |
| `Technical Writer` | Actualizar README y crear entrada inicial en AI Engineering log |

---

## Technical Plan

### 1. Backend — ASP.NET Core 9 + Clean Architecture

- Solución .NET con 4 proyectos (WebAPI, Application, Domain, Infrastructure)
- EF Core + SQLite en Infrastructure, Identity + JWT en WebAPI
- Healthcheck endpoint sin lógica de negocio

### 2. Frontend — Vite + React 18 + TypeScript

- Vite + React 18 + TypeScript + Tailwind + shadcn/ui + Zustand + React Router

### 3. MCP Servers — Python + FastMCP

- workflow/mcp/biblioteca-mcp/server.py y workflow/mcp/security-audit-mcp/server.py con FastMCP

### 4. Database

- workflow/database/.gitkeep

### 5. Docker

- docker-compose.yml raíz con backend, frontend, chatbot y MCPs; Dockerfiles individuales

### 6. CI — GitHub Actions

- .github/workflows/ci.yml para build backend + frontend

---

## Validation Plan

| # | Validación | Comando | Resultado Esperado |
|---|---|---|---|
| 1 | Compilación backend | `dotnet build` | 0 errores |
| 2 | Build frontend | `npm run build` | 0 errores |
| 3 | Docker | `docker compose build` | Imágenes OK |
| 4 | MCP archivos | Verificar server.py | Existen |
| 5 | Healthcheck | `curl http://localhost:5000/health` | HTTP 200 |

---

## Documentation Plan

1. Actualizar README.md raíz
2. Crear entry en AI Engineering log
3. Actualizar DECISIONS.md si surge

---

## Lifecycle Updates Required

1. Avanzar estado a 'Planned'
2. Tras aprobación, avanzar a 'In Progress'
3. Tras implementación, avanzar a 'Implemented'
4. QA validará y avanzará a 'Validated' o 'Rejected'

---

## Implementation Notes

### Initial implementation (approved plan)

- Se crean los 4 proyectos .NET (Web, Core, Data, Infrastructure) y se registran en red
- Bibliotecas EF Core SQLite factory de initial seed

### Rework March 2026 (Rejected -> In Progress)

- `workflow/backend/src/WebAPI/appsettings.json`: se eliminarion keywords no soportados por Microsoft.Data.Sqlite (`Cache Size`, `Journal Mode`, `Busy Timeout`). Connection string local: `Data Source=../database/BibliotecaVirtual.db;Pooling=True`.
- `workflow/backend/src/WebAPI/Program.cs`: se añadió `ResolveLocalSqlitePath` para resolver rutas relativas a `workflow/database/` del repo y se registró el interceptor en `AddDbContext`.
- `workflow/backend/src/Infrastructure/Services/SqlitePragmaInterceptor.cs` (nuevo): aplica `PRAGMA journal_mode=WAL` y `PRAGMA busy_timeout=5000` sobre cada conexión, preservando los parámetros de `appsettings.json`.

---

## QA Result

**Resultado: REJECTED**

| # | Validación | Resultado Real |
|---|---|---|
| 1 | dotnet build | PASS (0 errors; NU1903 warning) |
| 2 | npm run build | PASS |
| 3 | docker compose config | PASS |
| 4 | MCP archivos | PASS |
| 5 | Healthcheck | FAIL — crashes al iniciar |
| 6 | EF Core SQLite | FAIL — no se crea la BD |

Motivo: keyword `Cache Size` no soportado. Se corrigió en rework; pendiente re-validación completa.

---
